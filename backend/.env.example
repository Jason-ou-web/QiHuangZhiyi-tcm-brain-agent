# === 必填环境变量 ===
# DeepSeek API Key（从 https://platform.deepseek.com 获取）
DEEPSEEK_API_KEY=sk-your-api-key-here

# JWT Secret（用于签名 Token，生成命令: openssl rand -hex 32）
# 注意：不要与任何已泄露的密钥相同
JWT_SECRET=your-jwt-secret-here

# === 可选环境变量（以下均为默认值） ===

# 服务端口
SERVER_PORT=8080

# DeepSeek API 配置
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Qdrant 向量数据库
QDRANT_HOST=localhost
QDRANT_PORT=6334
QDRANT_COLLECTION=tcm_knowledge

# Redis 缓存
REDIS_ADDR=localhost:6379
# REDIS_PASSWORD=

# Embedding 服务（Python embed_server.py，默认端口 8081）
# 启动方式: python embed_server.py
EMBEDDING_URL=http://localhost:8081/embeddings

# Reranker 服务（与 Embedding 共用 embed_server.py，默认端口 8081）
# 启动方式: python embed_server.py（同一进程同时提供 /embeddings 和 /rerank）
RERANKER_URL=http://localhost:8081/rerank

# CORS 允许的前端来源（多来源逗号分隔）
ALLOWED_ORIGINS=http://localhost:3000
